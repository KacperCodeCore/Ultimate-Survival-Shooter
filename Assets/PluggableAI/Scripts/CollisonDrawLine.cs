using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CollisionDrawLine : MonoBehaviour
{
    [SerializeField] private Color _color;
    //void OnTriggerEnter(Collider other)
    //{
    //    ContactPoint contact = other.ClosestPoint(transform.position);
    //    Debug.DrawLine(contact.point, contact.point + contact.normal, _color, 32, false);
    //}
}
